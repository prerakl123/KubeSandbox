apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-demo-echo
spec:
  selector:
    app: demo-echo
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
